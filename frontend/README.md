This is a minimal static frontend scaffold for development.

Install dependencies and start a static server:

```bash
npm install
npm start
```

The frontend proxies API requests to `/api/*` by default when using the Compose network; see infra/docker-compose.yml.